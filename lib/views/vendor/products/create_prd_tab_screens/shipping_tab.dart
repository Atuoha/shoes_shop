import 'package:flutter/material.dart';

class ShippingTab extends StatelessWidget {
  const ShippingTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text('Shipping Tab'));
  }
}

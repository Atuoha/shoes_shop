import 'package:flutter/material.dart';

class ImageUploadTab extends StatelessWidget {
  const ImageUploadTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text('Image Upload Tab'));
  }
}

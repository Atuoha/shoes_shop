import 'package:flutter/material.dart';

class AttributesTab extends StatelessWidget {
  const AttributesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text('Attributes Tab'));
  }
}
